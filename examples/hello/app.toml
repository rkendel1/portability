name = "hello"
version = "0.1.0"
[build]
source = "src"
entry = "src/main.rs"
[runtime]
kind = "wasm"
[http]
listen = 8080
[capabilities]
network = false
filesystem = true
[storage]
path = ".app/data"
mount = "/data"
