name = "hello-go"
version = "0.1.0"
[build]
source = "."
language = "go"
toolchain = "go"
entry = "main.go"
target = "wasm"
[runtime]
kind = "wasm"
[http]
listen = 8081
[capabilities]
network = false
filesystem = true
[storage]
path = ".app/data"
mount = "/data"
